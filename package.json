{
  "name": "ba-foods-web",
  "version": "1.0.0",
  "description": "B2B Export Portal for BA Foods, a division of BA Farms.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cd smartherd-portal && npm install && npm run build && mkdir -p ../dist/smartherd-portal && cp -r dist/* ../dist/smartherd-portal",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.5.0"
  },
  "dependencies": {
    "nodemailer": "^6.9.13",
    "pg": "^8.11.3"
  }
}
